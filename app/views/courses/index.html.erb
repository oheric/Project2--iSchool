<h1>Courses</h1>
<table>
<thead>
  <tr>
   <th>Your Courses</th>
   <th>Schoolyear</th>
  </tr>
</thead>
<%@courses.each do |c| %>
   <tr>
   <td><%= c.name %></td>
   <td><%= c.schoolyear %></td>
  


 <td><%= link_to 'Roster', show_course_path(:course => c.id) %></td>

 <td><%#= link_to 'Delete Student', delete_courses_path(:course => c.id), method: :delete, data: {confirm: 'Are you sure?'} %></td>

   </tr>
<% end %>
</table>

<%= link_to 'Add a Course', new_course_path %><br>




